const currency = require('./currency');
const document = require('./document');

const { Operation, OperationFact } = require('./base');


module.exports = {
    currency,
    document,
    Operation,
    OperationFact,
};